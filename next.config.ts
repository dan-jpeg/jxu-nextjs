import type { NextConfig } from "next";

const nextConfig = {
    images: {
        domains: [
            'edie-xu-portfolio.s3.us-east-2.amazonaws.com',
            'firebasestorage.googleapis.com'
        ],
    },
};

export default nextConfig;